{
  "name": "apds7311-payments-portal",
  "version": "1.0.0",
  "private": true,
  "description": "APDS7311 — Customer International Payments Portal — ST10541838",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "dev:backend": "cd backend && node server.js",
    "dev:frontend": "cd customer-portal && npm run dev",
    "install:all": "cd backend && npm install && cd ../customer-portal && npm install",
    "build:client": "cd customer-portal && npm ci && npm run build"
  }
}
